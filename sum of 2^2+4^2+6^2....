#include<stdio.h>
int main()
{
	int sum=0,i,t,n;
	printf("enter the value of n:");
	scanf("%d",&n);
	for(i=2;i<=n;i++)
	{
		if(i%2==0)
		{
			t=i*i;
			sum=sum+t;
		}
	}
	printf("sum of square even number:%d",sum);
}
